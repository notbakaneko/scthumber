version: '3.4'

services:
  http:
    build: .
    ports:
      - "${HTTP_PORT:-4001}:4001"
    volumes:
      # override packaged /app for dev
      - .:/app
      # use node_modules from the container, not the host.
      - node_modules:/app/node_modules

volumes:
  node_modules:
